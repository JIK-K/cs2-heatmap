# docker-compose.yml
version: '3.8'

services:
  cs2-heatmap:
    build: .
    container_name: cs2-heatmap
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - TZ=Asia/Seoul
    restart: always
